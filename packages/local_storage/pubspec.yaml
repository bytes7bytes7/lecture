name: local_storage
description: Local storage package.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  # local
  common:
    path: ../common

  # third party
  sembast: ^3.2.0+1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
